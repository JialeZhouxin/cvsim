# 心智模型校准文档

> 本文件是 AI 与用户之间的认知校准参考（mental model calibration）。
> 用途：让 AI 用**用户的认知框架**解释、教学、决策——而不是 AI 的默认框架。
> 来源：2026-08-13 访谈 Q1–Q12 逐项锁定，用户确认"够了"。
> 术语（说什么）见 [CONTEXT.md](./CONTEXT.md)；本文档管"怎么想、怎么说"。

---

## 0. 顶层协议：结构映射

**解释任何逻辑之前，先把新概念映射到用户最舒适的框架槽位，再从那里展开。**

解释一个新概念时，依次回答这四个问题（答完才展开细节）：

1. **它在实验流水线的哪一环？**（源 → 操作 → 测量）
2. **它属于哪个表示？**（Gaussian / Fock / Bosonic）四维切分里占哪格？
3. **它的直觉图像是什么？**（身体化 / 空间化类比，不是术语）
4. **怎么对账验证？**（哪条数值路径能证明它对了）

禁止用 AI 默认教科书顺序（定义 → 性质 → 例子）直接展开。

---

## 1. 领域认知（A 层，主）

### 1.1 三表示 = 同一物理的不同窗口

Gaussian / Fock / Bosonic 描述同一套 CV 物理，靠 Wigner 函数 / 密度矩阵相连。
差异用**四个维度**切分（用户亲自补全的完整维度集）：

| 维度 | Gaussian | Fock | Bosonic |
|------|----------|------|---------|
| 结构 | V, r̄（协方差 + 均值） | 截断振幅 / ρ | {(V_k, r̄_k, w_k)} 分量混合 |
| 成本 | O(m²) | N^m（指数） | O(K·m²) |
| 光学操作 | 高斯门 / 通道 | 任意门（截断内） | 复用 G 门 + 分量权重 |
| 输出形式 | 连续（homodyne/heterodyne） | 计数（PNR/截断概率） | 混合观测 |

### 1.2 中心锚点：实验流水线

一切从**实验叙事**出发：光源 → 光学操作 → 测量。
表示只是同一台实验的三种记账方式。讲物理永远从实验场景进入，不从表示进入。

### 1.3 教学：场景驱动

按具体实验组织讲解（相干探测、TMSV、GBS……），每个场景里三表示**对比出现**、数字对账。
不按表示排序教学（不写"先学 Gaussian 再学 Fock"式的教材结构）。

### 1.4 表示选型规则

**首选判据：测量类型。** 次级判据：精确度需求。

| 场景 | 表示 |
|------|------|
| homodyne / heterodyne（连续输出） | Gaussian |
| PNR / 光子计数 / 截断投影 | Fock |
| 混合观测 / 纠缠教学 | Bosonic |
| 要解析闭式 | Gaussian（精确） |
| 要数值精确截断 | Fock（注意 cutoff 泄漏） |

---

## 2. 方法论（B 层，次）

### 2.1 学习顺序：直觉 → 数学 → 代码

接收新概念的顺序固定：先要物理图景（具身/空间直觉）→ 再推导（数学）→ 最后看实现验证（代码）。
与教学输出同构：教程 = 物理动机 → 数学推导 → 可运行代码 → 可视化。

### 2.2 验证理解：数值对账为主，输出为辅

- **主**：同一物理走不同路径（G/F/B 三表示、闭式 vs 数值），数字一致到 atol 级才叫"对了"。
- **辅**：写出来 / 讲出来。能写成教程讲清 = 理解。
- 对账是理解与否的裁判；测试套件是工程固化手段，不是理解本身。

### 2.3 决策：维度表驱动为主，小步迭代兜底

- **主**：列选项 → 列比较维度 → 逐维打勾 → 拍板。给出决策时请配"推荐答案 + 权衡"结构。
- **兜底**：不确定时小步做、用结果反馈再调，不一次定死。

---

## 3. 协作协议（C 层，附注）

### 3.1 表达：逻辑语言 + 具身认知

- **逻辑语言**：因果链讲清"为什么"，先结论后论据，说人话。
- **具身认知**：抽象物理量配身体化/空间化直觉（视觉图景、手感、运动感、力）。
  例：压缩 = 橡皮泥被压扁；截断 = 手电筒照不到的部分看不见；Wigner = 相空间地形图。

### 3.2 反模式清单（禁用 → 替代）

| 禁用 | 替代 |
|------|------|
| 堆 API 签名 / 返回值 / 数据结构（"这个函数返回 ndarray"式） | 说它**做什么**、在流水线哪一环、为什么存在 |
| 工程术语压过物理（架构、依赖、抽象层次优先） | 物理语义优先，工程细节按需给 |
| 一上来就公式 / 代码，无直觉先行 | 直觉图像 → 逻辑链 → 数学 → 代码 |
| 教科书顺序展开（定义→性质→例子） | 场景驱动 + 结构映射（见 §0） |
| 术语堆砌（"Kraus 算符作用于密度矩阵"） | 先给身体化图景，术语作标注出现 |

### 3.3 术语

中文交流；必要英文术语后加中文注释。术语边界见 CONTEXT.md。

---

## 4. AI 使用说明

1. 本文件是**每次解释的起点**，不是可选的参考——解释前先过 §0 四问。
2. 与新概念相关的既有框架槽位不存在时，显式说明"这是新槽位"，而不是硬塞进旧框架。
3. 用户补充新维度/新规则时，本文件应当被更新（维护权在用户与 AI 共持）。
4. 与 CONTEXT.md 分工：术语冲突找 CONTEXT，表达与认知方式冲突找本文档。
