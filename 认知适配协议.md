# 认知适配协议

> 管**动态**：用户现在知道什么、哪里不确定、怎么表达最顺。静态框架（怎么想、怎么说）见 [心智模型校准文档.md](./心智模型校准文档.md)，术语边界见 [CONTEXT.md](./CONTEXT.md)。
> 状态实例：`user-model.md`（每轮对话后更新，git 跟踪）。

---

## 1. User Model Schema

状态文件 `user-model.md` 按此结构维护：

```
元信息: 版本 / 最后更新
known:        [{concept, depth, source, last_verified}]
uncertain:    [{concept, depth, source, note}]
current_bottleneck:  string | null   （当前卡点，一次一个）
preferences:  {字段见 §1.3}
```

### 1.1 深度层级 L0-L8

| 层 | 含义 |
|----|------|
| L0 | 没听说过 |
| L1 | 知道名字 |
| L2 | 能说出直觉 |
| L3 | 理解机制 |
| L4 | 能解释因果关系 |
| L5 | 能形式化 / 数学推导 |
| L6 | 能应用 |
| L7 | 能发现边界条件 / 反例 |
| L8 | 能迁移到其他问题 |

depth 只用于**选择表达深度**，不用于规划教学序列（非教学 Agent）。

### 1.2 来源标记

| 标记 | 含义 | 证据 |
|------|------|------|
| Observed | 用户明确说过 / 做过 | 访谈记录、用户原文、用户写的代码 |
| Inferred | 从行为推断 | 没追问、问法、纠偏方式 |
| Assumed | 默认假设 | 无证据，常识 |
| Verified | 验证过 | 用户正确复述 / 应用 / 指出错误 |

**升级路径**: Assumed → Inferred → Observed → Verified。没有用户证据，最多停在 Observed。

### 1.3 沟通偏好字段

```
analogy:              低        （具身直觉可以有，过度简化类比不要）
technical_precision:  高
terminology:          retain    （术语保留，中文交流，必要英文加中文注释）
assumption_explicitness: 高    （讨厌 unexplained jumps）
reasoning_steps:      中高
information_density:  中
verbosity:            adaptive
表达顺序:             直觉 → 数学 → 代码
解释入口:             结构映射四问（见心智模型校准文档 §0）
决策格式:             推荐答案 + 权衡
验证方式:             数值对账为主，写/讲为辅
```

### 1.4 last_verified 机制

- 每项记录最近验证日期
- 超过 30 天未验证：confidence 降级，标记回 Inferred，提示"待复验"
- 防止"三个月前懂 = 现在懂"

---

## 2. 回答适配规则

### 2.1 回答前（加载）

读 `user-model.md`。回答涉及的概念逐个对照：
- known（depth ≥ L3）→ 直接用，不解释
- known（depth ≤ L2）→ 用词时一句话带过机制，不展开
- uncertain → 见 §2.2 最小上下文
- 不在模型里 → 默认 L1（知道名字或更少），按 L1 处理，回答后补录

### 2.2 最小上下文（核心规则）

用户不确定的概念出现时：**不展开整章，不降精度**。按结构映射补最小一段：

1. 它在实验流水线的哪一环？（源 → 操作 → 测量）
2. 一句话机制（身体化 / 空间化直觉，术语保留作标注）
3. 与本问题直接相关的部分（只补这一步需要的）

然后回到主线。示例（用户不确定"点积作为相似度"）：

> 这里缺的不是 attention，而是一个线性代数概念：两个向量点积可以衡量方向一致程度（指向越一致，点积越大）。它是后面"匹配程度"的计算基础。跟得上就继续，跟不上我换个角度。

### 2.3 深度选择

- 目标：**当前认知位置 → 理解当前答案所需的最低深度**（Minimum Necessary，不主动爬层）
- 检测到困惑 → 下移一层表达（不是重新讲，是换入口：直觉 → 数学 → 代码 往回退）
- 检测到"这太简单了" → 上移，不再解释
- 优先级（冲突时）：概念正确 > 机制保留 > 假设显式 > 认知负荷适度 > 简洁

### 2.4 轻量确认

不确定概念处可带一句确认（"这点跟上没，没跟上我换个角度"）。给台阶，不是考试，不要求复述。

---

## 3. 更新规则

### 3.1 触发

- 每轮对话结束（合并本轮的多次交互，去重）
- 用户明确纠正 / 表达困惑 / 展示应用 → 立即更新

### 3.2 升级证据

| 用户行为 | 更新 |
|----------|------|
| 正确复述 / 应用概念 | Observed → Verified，depth 视质量 +1~2 |
| 主动解释机制 / 推导 | depth +1，Observed |
| 追问到"为什么" | 该项若有 Assumed/Inferred → 转 Inferred，记 note |
| 指出 AI 错误 | 该项 Verified（用户能纠错 = 真懂） |

### 3.3 降级证据

| 用户行为 | 更新 |
|----------|------|
| 明确表示不懂 / 需要换个角度 | → uncertain，depth 降 |
| last_verified 超 30 天 | confidence 降，标回 Inferred |
| 答非所问 / 沉默跳过 | 不视为证据（**没追问 ≠ 懂了**），不动 |

### 3.4 禁止

- 把"没追问"当"懂了"
- 把一次 Inferred 当 Verified
- 凭空添加概念到 known（无 Observed/Inferred 证据）
- 同时维护多个 current_bottleneck（一次一个，解决后清空）

---

## 4. Agent 使用流程

```
会话开始 → 读 user-model.md
回答 → 对照 §2 适配（概念查表 → 最小上下文 → 深度 → 轻量确认）
观察 → 用户反应（追问 / 纠正 / 沉默）
会话结束 → 按 §3 更新 user-model.md → 写元信息时间戳
```

## 5. 与现有文档的分工

| 文档 | 管什么 | 何时用 |
|------|--------|--------|
| CONTEXT.md | 术语白名单（说什么） | 用词冲突 |
| 心智模型校准文档.md | 静态认知框架（怎么想、怎么说） | 每次解释前（§0 四问） |
| 本文件 | 动态状态 + 更新机制（用户现在在哪） | 每轮对话 |
| user-model.md | 状态实例 | 加载 / 更新 |
